# LOE End-to-End Grade — Job 784140 (VAL261101 Westside Mall)

**Method:** rendered the active Supabase template `LOE-07-1 v7` against the real stored job data (job_loe_details + job_property_info.status_of_improvements + job_submissions), replicating generateLOEHTML's V07 mapper + §10 logic. Date 2026-06-10, qa-agent.

**Job inputs (live, confirmed):** statusOfImprovements="Improved - Under Renovation" (job_property_info), authorizedUse="Estate Planning" (no insurance override), stored value_scenarios="As-Is, As If Complete & Stabilized", approaches_to_value="Direct Comparison, Income, Cost", fee=6000, property=2129 Broadway Court/Industrial, client=Edward Johnson/Evergreen Holdings.

## Scorecard

| Check | Result | Detail |
|---|---|---|
| §10 derived scenarios (content) | **PASS** | Row1=As-Is, Row2=As If Complete & Stabilized — exact cascade match for "Improved - Under Renovation". |
| §10 EA/HC narratives | **PASS** | Both render REAL summary text (library built for both), not literal brackets. |
| §10 row cleanliness | **FAIL** | Template has 6 physical `<tr>` rows, NO `<!-- EAHC-ROW-n -->` suppression fences → rows 3–6 ship as **4 EMPTY table rows**. |
| Summary [ValueScenarios] | **PASS** | "As-Is, As If Complete & Stabilized" — stored populated, matches derived. |
| Summary [ApproachestoValue] | **PASS** | "Direct Comparison, Income, Cost" — stored populated. |
| Approaches cascade | **PASS** | Matches STATUS_TO_APPROACHES. |
| Core fields | **PASS** | Fee $6,000, property address, client name, interest (Leased Fee) all filled. |
| Leftover unresolved tokens | **PASS** | 0 stray `[Tokens]` in output. |

## THE BUG (headline)

Active template `LOE-07-1 v7` is **missing the `<!-- EAHC-ROW-n:START/END -->` fence comments** that `applyEahcRowSuppression` (generateLOE.ts) greps to remove unused §10 rows. With 2 of 6 scenarios populated, the 4 unused rows are NOT removed → 4 blank rows render in the §10 EA/HC table. The code is correct; the deployed template lacks the markers it depends on. Fix = re-author the template's §10 rows with the fence comments (or switch suppression to match `data-token`/empty-cell detection).

## Side flags (non-blocking)

- **Latent narrative bug:** NARRATIVES "As If Complete & Stabilized" eaDetail/hcDetail (loeCascade.ts L70-71) are copy-pasted from "As If Vacant Land" (wrong body). Harmless now (template uses summary only; resolveNarrativeDetails unwired) — bites when [EADetail_n]/[HCDetail_n] tokens go live. Typo "makret" L63.
- **Data-consistency:** transaction_status differs across tables (job_property_info="Listed" vs job_loe_details="Under Contract"); zoning_status (property_info="Legal Conforming" — a STALE pre-re-option value — vs loe_details="In Place"). useJobData:213 overrides these from job_property_info, so the dashboard/LOE would surface the property_info value. Worth reconciling which table is source-of-truth for these 3.

**Last reviewed:** 2026-06-10 by qa-agent — rendered active template vs live job data.
